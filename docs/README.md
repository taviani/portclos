# Docs Portclos

Les **chats Cursor ne se partagent pas** d’une conversation à l’autre. Tout ce qui doit survivre (décisions produit, plans, « où on en est ») vit **ici**, dans le dépôt.

| Fichier | Contenu |
|---------|---------|
| [product/auth-session.md](product/auth-session.md) | Session durable (refresh) + biométrie + patch kde-auth |
| [product/patches/](product/patches/) | Patch companion pour `kde-auth` (`offline_access`) |

Quand une discussion Cursor aboutit à une décision : **l’écrire ici** (PR), ne pas compter sur l’historique du chat.
